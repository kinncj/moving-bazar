build_settings:
    verbose: true
    ignore: 
        - "vendor"
        - "assets"
        - "build"
        - "Tests"
        - "composer.phar"

setup:
    composer:
        action: "install --dev"

test:
    shell:
        command: "cd %buildpath%; make"
