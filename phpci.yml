build_settings:
    verbose: true
    ignore: 
        - "vendor"

setup:
    composer:
        action: "install --dev"

test:
    php_unit:
        allow_failures: true
    php_code_sniffer:
        standard: "PSR2"
